[project]
name = "python-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cobs-py",
    "msgpack>=1.1.2",
    "pyserial>=3.5",
]

[tool.uv.sources]
cobs-py = { path = "../../cobs/cobs_py" }
